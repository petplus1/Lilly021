const styles = theme => ({
  root: {
    height: "100%"
  },
  listItem: {
    cursor: "pointer"
  },
  newChatBtn: {
    borderRadius: "0px"
  },
  unreadMessage: {
    color: "red",
    // position: "absolute",
    top: "0",
    right: "5px"
  }
});

export default styles;
